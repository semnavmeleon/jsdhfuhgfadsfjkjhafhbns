# Инструкция по использованию Спектрального Джиттера

## Что такое спектральный джиттер?

**Спектральный джиттер** — это метод аудио-аугментации, который добавляет в аудиосигнал частотные провалы (notch-фильтры) на определённых частотах. Это создаёт характерные искажения спектра, которые могут быть полезны для:
- Обхода алгоритмов контент-идентификации
- Создания уникальных версий треков
- Тестирования устойчивости аудио-моделей

## Режимы работы

### 1. **Random (Случайный)** — режим по умолчанию
Автоматически выбирает случайные частоты из предустановленного пула и применяет к ним фильтры со случайными параметрами.

**Параметры:**
- **Кол-во провалов**: количество фильтров (можно дробное, например 3.5 → округляется до 4)
- **Аттенюация (dB)**: максимальное ослабление сигнала в dB (например, 15.0)

**Как работает:**
- Выбирается N случайных частот из пула: [120, 250, 400, 630, 800, 1200, 1600, 2000, 2500, 3150, 4000, 5000, 6300, 8000, 10000, 12500] Гц
- Для каждой частоты случайно выбирается ослабление от `max_attenuation/2` до `max_attenuation`
- Ширина полосы случайно выбирается от 0.1 до 0.3 октавы

**Пример настроек:**
```
Кол-во провалов: 5.0
Аттенюация: 15.0
Режим: random
```

---

### 2. **Fixed (Фиксированный)**
Использует заранее определённые частоты с одинаковыми параметрами для всех фильтров.

**Параметры:**
- **Частоты (через запятую)**: список частот в Гц, например `120, 630, 2000, 5000`
- **Аттенюация (dB)**: единое значение ослабления для всех фильтров
- **Ширина по умолчанию**: ширина полосы в октавах (обычно 0.2)

**Когда использовать:**
- Когда нужен детерминированный, воспроизводимый результат
- Когда вы знаете, какие частоты нужно подавить
- Для массовной обработки с одинаковыми параметрами

**Пример настроек:**
```
Частоты: 250, 800, 2000, 5000
Аттенюация: 12.5
Ширина по умолчанию: 0.2
Режим: fixed
```

---

### 3. **Manual (Ручной)** — максимальный контроль
Позволяет задать индивидуальные параметры для КАЖДОГО фильтра отдельно.

**Параметры:**
- **Частоты**: список частот через запятую (например, `120.5, 250.75, 400, 630.3`)
- **Ослабления**: список значений ослабления в dB через запятую (например, `5.5, 10.0, 15.2, 8.0`)
- **Ширины**: список ширин полос в октавах через запятую (например, `0.1, 0.2, 0.3, 0.15`)
- **Ширина по умолчанию**: используется для фильтров, у которых не указана ширина

**Важно:**
- Количество значений в списках может отличаться
- Если для фильтра не указано ослабление — используется `max_attenuation`
- Если для фильтра не указана ширина — используется `Ширина по умолчанию`

**Пример настроек:**
```
Частоты: 120.5, 250.75, 400, 630.3, 1200
Ослабления: 5.5, 10.0, 15.2, 8.0, 12.0
Ширины: 0.1, 0.2, 0.3
Ширина по умолчанию: 0.25
Режим: manual
```

В этом примере:
- Фильтр 1: 120.5 Гц, ослабление 5.5 dB, ширина 0.1 октавы
- Фильтр 2: 250.75 Гц, ослабление 10.0 dB, ширина 0.2 октавы
- Фильтр 3: 400 Гц, ослабление 15.2 dB, ширина 0.3 октавы
- Фильтр 4: 630.3 Гц, ослабление 8.0 dB, ширина 0.25 (по умолчанию)
- Фильтр 5: 1200 Гц, ослабление 12.0 dB, ширина 0.25 (по умолчанию)

---

## Рекомендуемые настройки частот

### Для мягкого воздействия (едва заметные искажения):
```
Частоты: 250, 800, 2000, 5000
Аттенюация: 3-6 dB
Ширина: 0.1-0.15
```

### Для среднего воздействия (заметные искажения):
```
Частоты: 120, 630, 1600, 4000, 10000
Аттенюация: 8-12 dB
Ширина: 0.15-0.25
```

### Для агрессивного воздействия (сильные искажения):
```
Частоты: 250, 400, 1200, 3150, 6300, 12500
Аттенюация: 15-25 dB
Ширина: 0.2-0.4
```

### Для обхода контент-идентификации:
```
Частоты: 300, 700, 1500, 3000, 6000
Аттенюация: 10-15 dB
Ширина: 0.2
Режим: fixed
```

---

## Поддерживаемые форматы чисел

✅ **Целые числа**: `120`, `250`, `400`
✅ **Дробные числа**: `120.5`, `250.75`, `630.33`
✅ **Отрицательные значения**: не поддерживаются для частот
✅ **Научная нотация**: не поддерживается

**Примеры корректного ввода:**
- `120, 250.5, 400, 630.75` ✓
- `5.5, 10, 15.2, 20` ✓
- `0.1, 0.2, 0.3` ✓

---

## Почему некоторые треки "калятся" сильнее?

1. **Спектральная плотность**: Треки с богатой гармонической структурой (оркестровая музыка, сложные аранжировки) более чувствительны к частотным провалам.

2. **Расположение частот**: Если частота джиттера попадает на основную гармонику инструмента или вокала, искажение будет очень заметным.

3. **Динамический диапазон**: Тихие треки с высоким динамическим диапазоном показывают искажения ярче.

4. **Количество фильтров**: Больше фильтров = больше искажений.

5. **Ширина полосы**: Широкие фильтры (0.3-0.5 октавы) влияют на больший диапазон частот.

**Совет**: Начинайте с малых значений (3-4 фильтра, 5-8 dB) и постепенно увеличивайте, пока не добьётесь нужного эффекта.

---

## Частые ошибки

❌ **Неправильно**: `120; 250; 400` (используйте запятые, а не точки с запятой)
❌ **Неправильно**: `120 250 400` (разделяйте запятыми)
❌ **Неправильно**: `abc, def` (только числа)
❌ **Неправильно**: `-120, -250` (частоты не могут быть отрицательными)

✅ **Правильно**: `120, 250, 400`
✅ **Правильно**: `120.5, 250.75, 400.0`
✅ **Правильно**: `120,250,400` (пробелы необязательны)

---

## Совместимость с другими методами

⚠️ **Конфликты:**
- **Spectral Masking + Spectral Jitter**: множественные провалы в спектре могут привести к чрезмерным искажениям
- **Concert Emulation + Mid/Side**: избыточная обработка

✅ **Хорошо сочетается с:**
- Temporal Jitter
- Phase Invert
- DC Shift
- Ultrasonic Noise

---

## Примеры готовых пресетов

### Пресет "Лёгкий VK"
```
Режим: random
Кол-во провалов: 3.0
Аттенюация: 6.0
```

### Пресет "Средний VK"
```
Режим: fixed
Частоты: 250, 630, 1200, 2500, 5000
Аттенюация: 10.0
Ширина: 0.2
```

### Пресет "Агрессивный"
```
Режим: manual
Частоты: 120, 300, 600, 1200, 2400, 4800, 9600
Ослабления: 12, 15, 18, 15, 12, 10, 8
Ширины: 0.2, 0.25, 0.3, 0.3, 0.25, 0.2, 0.15
Ширина по умолчанию: 0.2
```

### Пресет "Точечный удар"
```
Режим: manual
Частоты: 440, 880, 1760
Ослабления: 20, 18, 15
Ширины: 0.05, 0.05, 0.05
Ширина по умолчанию: 0.1
```
(Нацелен на ноту A и её гармоники)

---

## Технические детали

**Диапазон частот**: 120 Гц — 12500 Гц (в random режиме)
**Тип фильтра**: Equalizer с width_type='o' (октавы)
**Формула фильтра**: `equalizer=f=<freq>:width_type=o:width=<width>:g=-<attenuation>`

**Пример ffmpeg-фильтра:**
```
equalizer=f=250:width_type=o:width=0.2:g=-10, equalizer=f=800:width_type=o:width=0.15:g=-12
```

---

## Вопросы и ответы

**Q: Можно ли использовать нецелые значения?**
A: Да! Теперь поддерживаются дробные числа вида `120.5`, `250.75` и т.д.

**Q: Почему нельзя ставить отрицательные частоты?**
A: Частота — это физическая величина, которая не может быть отрицательной. Отрицательное ослабление (усиление) технически возможно, но не рекомендуется.

**Q: Сколько фильтров можно добавить?**
A: Технически ограничений нет, но более 10-12 фильтров приводят к сильным искажениям. В random режиме максимум — 16 (размер пула частот).

**Q: Как добиться воспроизводимого результата?**
A: Используйте режим `fixed` или `manual` — они дают детерминированный результат. Random режим всегда случаен.

**Q: Можно ли сохранить настройки?**
A: Да, используйте функцию сохранения пресетов в интерфейсе программы.
